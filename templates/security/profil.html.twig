{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="container">
            <h1> Bonjour {{ user.email }}</h1>

            <h2>Vos derniers commentaires sur Wild Séries :</h2>
            <ul>
                {% for comment in comments %}
                    <li>Episode : {{ comment.episode.title}} - Message : {{ comment.comment }} - Note : {{comment.rate}}/5 </li>
                {% endfor %}
            </ul>


        </div>
    </div>
{% endblock %}